# EXP-045 verdict

Status: **REFUTED overall** for `p=8,...,11`; P1, P2, and P3 fail as declared. A stronger stable
full-carrier structure is retained.

The exhaustive 256-projection lattice proves that the minimal full-Bockstein carriers are exactly
masks `59` and `62` at every tested parameter. Their intersection mask `58` carries Bockstein
ranks `1,2,3,5`, while either alternative completion adds exactly two classes and reaches the full
ranks `3,4,5,7`.

The minimal nonzero carrier changes from mask `58` through `p=10` to mask `56` at `p=11`, where
the triad `R3,R4,R5` first carries one class. This identifies a semantic threshold behind the
previous defect jump and redirects the proof toward relative integral presentations for
`58 -> 59`, `58 -> 62`, and `56 -> 58`.

The carrier lattice is exact and independently audited, but row projection is not a unimodular
normal form. No recurrence, all-parameter theorem, manuscript, or Zenodo update is claimed.
