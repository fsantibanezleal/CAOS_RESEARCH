# EXP-046 verdict

Status: **REFUTED overall** for `p=8,...,11`; P1 and P2 fail, while P3 passes finitely.

All sixteen row projections reduce to one connected nonzero core. The stable minimal full
carriers `59` and `62` admit zero unit-leaf cancellations, so their common defect-two completion
cannot be separated by leaf peeling into two defect-one blocks. This decisively closes the
cheapest proposed integral mechanism.

Mask `56` has defects `0,0,0,1`, while its normalized row aliases and column-atom set remain
unchanged. The `p=11` class is therefore a sign/rank threshold inside persistent support, not the
arrival of a new semantic atom.

The independent opposite-priority audit passes all 214 checks. The next proof gate is a
fill-producing relative integer reduction for `58 -> 59`, `58 -> 62`, and separately `56 -> 58`,
with explicit unimodular certificates. The finite obstruction does not establish a recurrence,
an all-parameter normal form, or the unresolved connecting-parity theorem, so it does not trigger
a manuscript or Zenodo update.
