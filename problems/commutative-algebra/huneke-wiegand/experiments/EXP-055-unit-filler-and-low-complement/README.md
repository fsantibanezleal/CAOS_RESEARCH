# EXP-055: unit filler and low-complement coordinates

Read [proof.md](proof.md) for the uniform filler and [verdict.md](verdict.md) for its limits.
The one-column correction repairs the full source identities at `p=8,9,10`; the fixed-high
slice opens the uniform formula proved separately in EXP-056.

From the repository root, run this folder's `run.py` and `audit.py` with `.venv/Scripts/python.exe`.
Both accept `--output` for temporary reruns. No HNF or full original basis is built.
