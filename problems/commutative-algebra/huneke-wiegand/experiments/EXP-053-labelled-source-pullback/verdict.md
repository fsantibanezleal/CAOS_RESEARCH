# EXP-053 verdict

Status: **REFUTED on P2; P1 PASSES FINITELY WITH A RESOURCE OVERRUN; P3 NOT EVALUATED**.

Exact labelled pullback succeeds at `p=8,9,10`, with unique column mappings and direct source-to-
relative identities. It reveals a relevant structural fact: both stable completions use the same
source chain. Their two EXP-052 boundary formulas are the two added-row restrictions of one cycle
over the common mask-58 kernel.

The generic HNF pullback is not a viable proof language. Its labelled supports are 125, 178, and
238 and contain 78 coefficient-sensitive semantic skeletons, refuting the declared bound twelve
despite coefficients remaining bounded by four. The `p=10` HNF completed only after overrunning
the safe-stage resource gate; `p=11` source labels stayed locked. A 62-check artifact/cross-
completion audit passes.

The strongest next route is a direct labelled telescoping construction for the combined
`58->63` boundary. One such identity would prove the first class in both completions
simultaneously. The second class, generic dual lower bound, and free-complement upper bound remain
open, so the all-parameter problem is not solved.

No manuscript or Zenodo update is triggered.
