# EXP-051 verdict

Status: **REFUTED overall**; P1 and P2 pass finitely, while P3 fails.

Both stable completion quotients admit extremely simple exact finite representatives before
canonical quotient normalization. For every `p=8,...,11`, two independent binary cycles satisfy

```text
Rz=2b,
```

with `support(z)<=6` in the primary route, `support(b)<=8p`, and every coefficient of `b` bounded
by two. A high-pivot/reversed-order route independently finds rank-two spans within the same
bounds. The 409-check audit reconstructs all 32 exact identities from the frozen raw matrices.

The sorted support sequences are not uniformly affine, so raw elimination indices and size fits
are not a proof language. The actionable result is instead structural: EXP-050's large corrected
canonical representatives are section artifacts. The next primal gate is to pull the one-, two-,
and six-column witnesses back to labelled source chains and test symbolic parameter formulas.
The support-four dual route remains strongest for the uniform lower bound; a relative-Morse or
explicit free-complement theorem remains necessary for the upper bound.

This is a relevant finite advance, not a solution of the all-parameter connecting-parity problem.
No manuscript or Zenodo update is triggered.
