# Attempt 005: the first new basis leaves common quadratic fibres

One new row basis covered all degree-3 and degree-6 modular probes at
\(Y=0,1\) over two primes per block. Exact reconstruction completed with
largest SCC 35. The normalized determinant is

\[
A^{85}H(X,B,Y),\qquad \deg_Y H=2,
\]

and passed a direct 125-by-125 determinant control.

Exact gcd arithmetic over both finite fields shows that every known section,
including the new one, has a common quadratic factor in \(Y\). Thus the
generic \(Y=0,1\) probes did not target the only unresolved fibres.

Redirect: use the persisted common quadratic in each block as the next
invariant. Select rows only at its finite-field roots. A full augmented-rank
drop there would expose a genuine residual stratum; otherwise one more exact
section should remove the common factor.

