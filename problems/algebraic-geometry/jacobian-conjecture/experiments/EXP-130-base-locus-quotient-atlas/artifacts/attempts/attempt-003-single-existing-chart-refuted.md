# Attempt 003: the single existing-chart prediction is refuted

After repairing coefficient extraction, the degree-3 block passed all
same-point gates: the reconstructed \(X\)-class annihilates both \(R\) and
\(S\), and its minimal polynomial matches the degree-3 \(X\)-projection
factor.

The EXP-124 section \(N(X,B)\), however, is not a unit on that block. This
refutes the opportunistic prediction that the single EXP-124 chart closes the
complete principal-open base locus.

Redirect: test every already reconstructed exact section that is independent
of \(Y=A^2C\). Such a section is valid uniformly in \(C\) at \(R=S=0\). Only
if those sections fail to cover a block should EXP-130 launch new modular row
selection.

